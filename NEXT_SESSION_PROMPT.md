# Kickoff prompt for the next session

Copy everything in the block below into the first message of the new session.

---

Continuing work on **PantryHub** — an iOS app (SwiftUI / Xcode) at
`/Users/bob/Documents/pantryhub v.0`. The front-end is largely complete; the
**backend is the next phase**.

Get oriented before doing anything:

1. Read **`HANDOFF.md`** in the project root — it has the full picture: file
   inventory, build & verify commands, design system, data model, the icon/image
   system, known issues, and the backend roadmap.
2. Follow my **auto-memory** (it loads automatically): I'm non-technical —
   explain things simply, don't overwhelm me, ask product/design questions not
   engineering ones, and make technical calls for me, stating them briefly.
3. Skim the codebase under `PantryHub/` using `HANDOFF.md` section 4 as the map.
   Core files to read first: `ContentView.swift`, `AppStore.swift`,
   `Models.swift`, `SampleData.swift`, `Theme.swift`, `Components.swift`.

Then **STOP**. Give me a short, plain-language summary of where the project
stands and confirm you're ready — and do **not** start any backend work,
refactors, or code changes until I give you a specific instruction.

---
